#include<stdio.h>
#include<conio.h>
int main()
{
 float a;
 printf("enter the floating point number:");
 scanf("%f",&a);
 printf("the round of floating point %f is %f",a,round(a));
 getch();
 return 0;
 }
